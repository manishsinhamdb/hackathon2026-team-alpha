# Copy to .env in this folder and fill in. Never commit the real file.
# Atlas Administration API key pair (digest authentication), project-scoped.
ATLAS_PUBLIC_KEY=
ATLAS_PRIVATE_KEY=
ATLAS_PROJECT_ID=
# LLM key for this agent. The shared services read the root .env separately.
LLM_API_KEY=
