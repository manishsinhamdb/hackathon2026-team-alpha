# Local dev settings only (see agent.yaml for app config).
# Under `agentic dev up --all` the shared playground UI is owned by chat-agent, so this agent takes a
# Docker-assigned playground port to avoid a port clash. Pin one here only for a single-agent `dev up`.
# services:
#   playground:
#     port: 3000
